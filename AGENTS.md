# Repository Agent Guidance

이 저장소는 SCAN 자산추적대회의 문제, 근거 자료와 재현 가능한 분석을 관리한다.
빠른 정답보다 검증 가능한 정답을 우선한다.

## Workflow

1. 문제 원문에서 요구값, 체인, 대상, 시점과 출력 형식을 확인한다.
2. 조사 경로가 필요할 때만 `route.md`를 확인하고, 선택한 route 문서만 읽는다.
3. 체인과 기준 블록을 고정한 뒤 조사한다.
4. 원시 자료, 해석과 추론을 구분한다.
5. 핵심 결론에는 출처와 재현 명령을 남긴다.
6. 조사 시작 시 `scripts/env_check.py`로 RPC와 조회 환경을 확인한다.
7. 반복적인 logs·receipt·trace 수집은 `scripts/rpc_collect.py`로 재현한다.

특정 체인, 도구와 문제 유형의 절차는 이 파일에 추가하지 않고 `routes/`에 둔다.
특정 문제만의 예외는 해당 문제의 조사 문서에 기록한다.

`env_check.py`는 Dune MCP를 설치하거나 인증하지 않는다. Agent runtime에서 확인한
Dune MCP 상태만 `DUNE_MCP_AVAILABLE` 또는 CLI flag로 전달하고, Dune이 없으면
route의 fallback 규칙에 따라 RPC를 사용한다.

## RPC

이미 설정된 RPC나 문제에서 지정한 RPC를 우선 사용한다. EVM RPC가 없다면
`https://chainlist.org/?testnets=true&search={chain}`에서 공개 후보를 찾는다.

공개 RPC는 읽기 전용으로 사용하고, 사용 전에 chain ID와 최신 블록을 검증한다.
과거 상태가 필요하면 기준 블록 조회와 archive 지원 여부도 확인한다. API 키는
환경변수로만 전달하고 저장소나 조사 기록에 남기지 않는다.

## Investigation rules

- 과거 문제의 `current`를 무조건 최신 블록으로 해석하지 않는다.
- 주소, 트랜잭션 해시와 원시 입력값은 원문을 보존한다.
- 정렬과 정규화는 문제에서 지정한 자료형과 규칙을 따른다.
- 수작업 합산, 필터링과 반복 조회는 작은 스크립트로 재현한다.
- Explorer 라벨 하나만으로 주소의 소유자를 확정하지 않는다.

독립 풀이 또는 사고과정 비교를 요청받으면 기존 정답, 풀이 메모와 외부 write-up을
먼저 읽지 않는다. 비교 단계에서만 기존 풀이를 확인한다.

## Agents and safety

서로 독립적인 조사 축이 둘 이상일 때만 하위 에이전트를 사용한다. 각 에이전트의
범위와 산출물을 분리하고 같은 파일을 동시에 수정하지 않는다.

기본 작업은 읽기 전용이다. 명시적인 요청 없이는 트랜잭션 전송, 서명, 승인,
자산 이동 또는 상태 변경을 수행하지 않는다.

## Done

체인과 기준 블록, 핵심 근거, 재현 방법과 요구된 출력 형식이 모두 확인되어야 한다.
